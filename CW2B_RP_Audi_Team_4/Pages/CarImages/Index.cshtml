@page
@model CW2B_RP_Audi_Team_4.Pages.CarImages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarImage[0].ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarImage[0].Has3DModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CarImage) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Has3DModel)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CarImageID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CarImageID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CarImageID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
